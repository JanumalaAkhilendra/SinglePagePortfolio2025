import React from 'react';
// Import the CSS file where you will place the styles from Uiverse.io
import './Loader.css'; 

/**
 * A simple loader component based on the Uiverse.io design by bociKond.
 * It's just a single div with the class "loader".
 */
const Loader = () => {
  return (
    <div className="loader">
      {/* The actual animation is created entirely using CSS */}
    </div>
  );
};

export default Loader;